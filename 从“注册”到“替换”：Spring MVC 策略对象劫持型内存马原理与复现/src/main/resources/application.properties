server.address=127.0.0.1
server.port=9091

# Initialize DispatcherServlet during application startup. Without this setting,
# a direct JSP request can arrive before DispatcherServlet initializes its strategies.
spring.mvc.servlet.load-on-startup=1
